services:
  nexusmind:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      ENVIRONMENT: container
    volumes:
      - nexusmind-data:/app/data
    restart: unless-stopped

volumes:
  nexusmind-data:
